# BookingTours hero film

12-second, 1920×1080, 60fps Remotion composition for the homepage hero.

```bash
npm install
npm run studio
npm run still
npm run render
```

The composition is `BookingToursHero`. Output is written to `out/`.
