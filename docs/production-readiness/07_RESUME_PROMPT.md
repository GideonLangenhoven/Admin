# Resume prompt — Sol/max

Select `gpt-5.6-sol` / `max`, then paste:

```text
Resume BookingTours production readiness in GideonLangenhoven/Admin using
docs/production-readiness/01_SOL_TAKEOVER.md. Verify effective Sol/max routing.
Required source: feature/launch-rollout-2026-09-20, originally inspected at
76d00f3157acb2c73159bef6b8d40a108a9e767d. Preserve accepted descendants and
unrelated dirty work; investigate unexpected branch or ancestry changes.

Read STATE.md, ISSUES.json, active packets, behavior/role/workload/release and
approval records in docs/production-readiness/. Read the release contract and
corrected backlog if new to this session; thereafter inspect only evidence
needed for the next task. Missing checkpoints mean G0 discovery, not assumed
completion. Do not repeat the full audit or reapply verified patches.

Verify candidate/configuration/fixture changes and invalidate affected evidence.
Check any ongoing process and real elapsed time; interrupted tests remain
incomplete until rerun under the approved protocol. Preserve partial logs.

Continue the first dependency-ready task: failing-before/passing-after
behavioral proof, minimal correction, affected tests and independent
Astra/high review where required. Resolve OPERATOR refund policy and existing
pricing migration scope before affected changes. Preserve existing accounts,
IDs, links, original payment modes, pending work and demo safeguards.

Continue independent isolated work around external blockers. Do not infer
production/financial/message/paid/load approval from available credentials.
Update the compact checkpoint with precise missing evidence or authority.
Do not claim ROLLOUT_READY_2000 without every release gate passing for the
exact candidate/workload and approved deployed canary. Do not promise work
will run after this session ends unless an actual monitored process exists.
```
