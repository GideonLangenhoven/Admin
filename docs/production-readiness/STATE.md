# BookingTours production-readiness checkpoint

Updated: 2026-09-23T07:52:00Z. Release verdict: **FAILED_GATE**. Routing resumed after the host model-switch event and the owner's continuation instruction: Astra handles orchestration/review, and one explicitly dispatched GPT-6 Sol Max worker handles implementation. Effective model/effort telemetry remains unavailable and is not claimed. Neither `IMPLEMENTATION_ACCEPTED_FOR_QUALIFICATION` nor `ROLLOUT_READY_500 — GO_TO_MARKET` has been reached.

## Exact source and deployed state

- Admin release branch `codex/bookingtours-simple-view-readiness-2026-09-20` started at `52afaff997a22e715e3d51194bd2ea07dfccbc7a` (tree `1f388e49894b024001c69d4620bfc384c11c7a69`); this checkpoint edits only readiness documents in its isolated worktree. The alias `admin.bookingtours.co.za` serves Vercel deployment `dpl_9e1NYwdPEsrXBeVQ3XX6DpQ6m6qL`; Vercel's read-only deployment API identifies deployed commit `ebb62b8f318d7039efcbf1b66d3f5bce9f02996d`, not the branch HEAD.
- Current companion remote `main` is `1cd80384884c5176f60607798b51fd54cf970fb2` (tree `eedc22e42cb50296e58ae00facc972cd2653eb78`). The deployed booking alias serves `dpl_Gb2ULvtZBjNej8STiNyejxfdbc5i`. Earlier release records identify its source as mobile branch `2ff1d82046c73202c3146d11299cff9bc379ac54`; Vercel did not expose a Git source fingerprint for this deployment, so that mapping still needs independent confirmation. Current main does not contain the seven-file mobile commit.
- A separate enabled onboarding app serves `onboarding.bookingtours.co.za` from Vercel deployment `dpl_AUuC2Hb6ArcHKuPGNqnJS7p4Te6T`. Its linked repository is `GideonLangenhoven/onboarding`, whose current `main` is `4ab0d6993dc8c1f1d2fc1b82fc58e117a5f26616` (tree `7a44195fd310abc126419f89e0d181bcee76a185`, lockfile SHA-256 `430d95e5f46d4d2b2d8eadc94b9e3870f68caa135d00d792ab334f357cff22cf`). This deployment was made via CLI and exposed no Git commit metadata, so exact deployed source remains unverified.
- The user's main `/Users/gideonlangenhoven/dev/capekayak` checkout has unrelated uncommitted UI/migration/test work. It was not changed. Isolated C02 and companion C01 worktrees are `/private/tmp/bookingtours-c02-20260923` and `/private/tmp/bookingtours-booking-c01-20260923`; both are source work only.
- Admin lockfile SHA-256 is `932b03d227b35f42c604bd5ee4b0c9ada30a1805f9bc9e3962b206d656723534`; current companion main's pre-correction lockfile SHA-256 is `18df6c2fb28d06c71b69d8a4f31d205e564169def46238a111cfc7a059db5cac`. The dashboard snapshot and hot-path admission migration file hashes are `b2c17b125be9d94bd8f9f7b14b601c70d2997e6c645cf484a6fb27df403eb515` and `fbd83f188e52181f1d173d92e1d89732f943e5901e5942ff87477d531d06b2a0`. The remote ledger must be rechecked before any further migration action.

## Routing and in-flight corrections

The local `codex-cli 0.156.1` configuration reads `gpt-6-sol`/`max`; it does not establish this embedded session's effective routing. The supported collaboration host accepted explicit dispatches for two separate `gpt-6-sol`/`max` implementation workers (C02 Admin and C01 companion) and separate read-only `gpt-6-astra`/`xhigh` release/review workers. Effective runtime model/effort metadata is not exposed. Requested and accepted dispatch are recorded; observed effective routing remains unknown. The host subsequently signalled a model switch and the owner instructed continuation with Astra orchestration and Sol Max implementation. Repeated routing confirmation is not needed; requested settings and the limited host observations remain separate.

Fresh read-only npm audits on 23 September found zero entries in the Admin lockfile, 17 in the current and historically deployed companion lockfiles (one critical, eight high), and 14 in the enabled onboarding lockfile (one critical, nine high). The companion directly pins Next 16.1.6, Sharp 0.34.5, PostCSS 8.5.8, and DOMPurify 3.3.3; onboarding directly pins Next 16.1.6 and PostCSS 8.5.8. The isolated companion C01 patch is committed as `34cc24c966436763e0acd6beb5db37cc860ef725`: deterministic install, full and production-only audits with zero findings, TypeScript and Webpack production build pass. Its existing lint gate has 85 findings. Fresh-context review dispatched as Astra xhigh **rejected** deployment because this current-main-based candidate loses historical checkout ownership proof, multi-leg combo/payment modes, tenant-scoped chat, Paysafe CSP, safe cancellation recovery and image-proxy SSRF guards. A synthetic no-network image-route test followed an allowed-origin redirect to loopback and returned a public cached image. The narrow follow-up patch was stopped before edits; the C01 worktree is clean. Onboarding remains untouched. The 22 September Next.js `next/og` advisory is a separate applicability check; no `next/og` or `ImageResponse` import was found in Admin or companion source. The isolated C02 Admin patch is committed as `45e07d8` plus correction `b4439f7`; its first review found old-tab compatibility and reset-bucket defects, which the second commit corrected. Fresh-context review dispatched as Astra xhigh **accepted** the corrected head after 202 focused tests, old/current AuthGate compatibility and actual installed Next `after()` lifecycle checks. C02 is integrated locally at 739a3fe6f1706e8fb12ee86e654a5617a1cafdfb; 46 affected tests passed after integration, and the application tree exactly matches the reviewed C02 head. It is not deployed.

## Evidence preserved and gates still open

The dated local Auth, MFA, refund, arrivals, notification, pricing and Simple View regressions in this directory remain evidence for their unchanged scope. They are not proof for later code, dependencies, deployments or full product behavior. The earlier 500-session read smoke passed. The first mixed-staff smoke failed and was aborted; the first corrective smoke failed again with target-side origin latency. The second admission correction is deployed at `ebb62b8`, but its recorded smoke is pending. No 60-minute mixed run, 2x spike, 24-hour continuous soak, isolated restore, approved production canary, or go-to-market gate has passed. The previous bounded smoke window ended on 22 September at 16:00 UTC.

The linked Supabase project `ukdsrndqhsatjkmxijuj` is active in `eu-west-3`. A read-only 23 September count found 3 businesses, 2 admin users, 6 tours, 50 bookings, 180 slots, 50 customer rows and no `wa_messages`. The owner confirmed the 50 booking and customer rows are replaceable synthetic/demo data. The additional business/slot provenance and exact current hosted action permission still require verification; preserve every row until then. Local environment inspection found access for read-only Supabase queries but did not establish current provider sandbox credentials or distributed Redis configuration. The owner confirmed the prior private recipient/monitoring destinations remain approved for planning, and a ZAR 0 ceiling applies. On resumption the owner supplied the source project URL as the restore target and excluded the other visible project. A separate restore target is therefore not identified; do not inspect or use other projects. No recipient values or credentials were exposed.

## Consolidated external prerequisite packet

No consequential hosted action is approved by this checkpoint. Before the next hosted test, the owner must identify the exact target and current data classification; the specific permitted mutation/load, provider test mode and recipient allowlist; an execution window; a budget ceiling and quota/compute plan; and abort/recovery steps. Volume traffic requires deployed provider doubles, negative outbound-guard proof, representative data, real Realtime topology, and an independent generator. The documented ZAR 0 ceiling cannot be treated as paid-capacity authority. Genuine Yoco test-mode, email, WhatsApp and OAuth/Drive journeys need current sandbox credentials and scoped recipients. Alert drills need an approved receiver and incident owner. Restore needs a separate isolated project with messages, payments and jobs blocked, plus database, Auth, object and protected-config access. Historical payment-record mutations, remote migrations, deployment/push, 24-hour canary, and public launch each need their own exact candidate/target/side-effect approval packet at the action stage. Earlier repository approval flags are historical evidence, not fresh blanket authorization.

Next: Sol is merging the full historically reviewed companion descendant into C01, preserving new dependency pins and current-main fixes, then addressing the six review findings with executable regressions. Review the result before updating the companion CI pin. Correct the separately enabled onboarding dependencies next, then continue C03-C06 and V01-V06. Do not start the qualification clock or mutate the hosted project from this checkpoint.
