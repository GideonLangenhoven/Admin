GUIDE-OFFLINE-SIMPLE-02 round7: CHANGES_REQUIRED

**P2 — Shared logout reports success when the SDK retains the authenticated session.** `components/AuthGate.tsx:310` ignores the `{ error }` returned by `supabase.auth.signOut()` and swallows thrown errors. `clearSession` then clears UI hints, returns `true` (`:336`), and the newly connected `components/SignOutButton.tsx:20-22` reloads without displaying its failure message. An actual-handler-chain probe reproduces both returned-error and thrown-error cases: durable worker revocation runs, the SDK session remains, and reload still occurs with no visible error. Independently, the installed Supabase SDK with a synthetic fetch returning HTTP 503 produces `AuthRetryableFetchError` and retains its persisted session (`installed-sdk-probe.cjs`/`.log`). Its installed `_signOut` implementation returns before `_removeSession` on that error. Thus a failed/offline logout can appear complete while authenticated SDK credentials remain available.

Minimum correction: preserve the successful durable queue tombstone, but treat SDK rejection or a returned SDK error as failed logout completion. Keep sufficient session state for retry; return `false` with an error and do not navigate or report successful logout while the SDK session remains. Add connected handler-chain regressions covering both SDK failure forms, alongside successful logout and failed durable revocation. This finding does **not** claim that the durably revoked worker can replay under another account.

Prior round6 failures are fixed: actual-module probes observe zero credential posts after delayed registration/unmount; no-Background-Sync foreground fallback reaches five attempts, preserves the stable event as `retry_exhausted`, stops timers, and exposes Retry; successful shared logout revokes before SDK sign-out/navigation, while failed revocation prevents navigation.

Independent validation: focused **106/106**, worker syntax, and scoped whitespace pass. All eight frozen source hashes match before/after review, including unchanged `/api/guide/check-in`. Real Chromium/native IndexedDB, service worker and Web Locks probes pass **9/9** with 15 local synthetic requests: canonical-only automatic deletion, partial-count payload/stable IDs, retained 409, bounded 429 and explicit retry, retained 401/token refresh, account/business isolation, same-owner relogin, two-tab durable clear, delayed 401, stale epoch rejection, and suppression of delayed canonical updates after account switch. See `browser-probes.cjs`/`.log` and `adversarial-probes.cjs`/`.log`. No app source edits or commits.

Limits: synthetic endpoints and SDK responses; no provider Auth, staging, full React browser flow, payment/waiver transaction, deployment or remote data validation. Existing 172/172 and TypeScript results were inspected in supplied validation, not rerun. Unrelated MFA/refund work is excluded; the unrelated refund commit advanced HEAD during review while frozen hashes stayed unchanged.

Routing requested: fresh Astra/xhigh independent review; Sol/max implementation. Observed: parent reports local `turn_context` model `gpt-6-astra`, effort `xhigh` for this reviewer; this is metadata, not backend attestation. Implementation routing was not independently observed.
