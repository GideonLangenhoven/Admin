# Corrected branch findings and implementation order

> Current-session override (21 September 2026): start with [00_START_HERE.md](00_START_HERE.md) and [STATE.md](STATE.md). Their source, routing, resolved policy and evidence supersede historical values below. Customer-protection and release-qualification gates remain in force.

Source: `GideonLangenhoven/Admin`, `feature/launch-rollout-2026-09-20`, inspected commit `76d00f3157acb2c73159bef6b8d40a108a9e767d`. Prepared 20 September 2026.

The local branch/HEAD and all 17 file blob hashes from the supplied source manifest matched during review. An independent Astra/high source review checked refund authorization, attribution, pending results and browser bulk behavior. No application tests, live database inspection, provider operations, load, restore or deployment were performed. Remote HEAD and current Actions history were not reverified. The original report's zero branch Actions runs is historical evidence only. Revalidate against actual execution HEAD; nothing here is verified fixed.

## Decisions that must precede affected mutations

- **Refund policy:** `docs/USER_MANUAL.md` section 2 permits Operator refunds; its refund section says Main Admin. `docs/admin-help/refunds.md` declares `required_role: OPERATOR`; `docs/admin-help/navigation.md` includes refunds for OPERATOR and ADMIN. `docs/SECURITY_MODEL.md` has a narrower older role table but lists batch-refund for any admin. `docs/TECHNICAL_SPEC.md` requires legacy ADMIN compatibility. Record and resolve the intended current policy. Do not equate missing exact allowlists with proof that every OPERATOR refund is unauthorized. Do not use `isPrivilegedRole` blindly for refund authority.
- **Commercial migration:** `supabase/migrations/20260920090000_enforce_standard_plan_pricing.sql` changes existing subscriptions to Standard, rewrites pending/active recurring amounts, and cancels open setup fees. Establish ledger/deployment state, approved terms and affected customers before applying/reapplying or altering it. Branch inclusion is not production approval. Do not silently reverse already-approved commercial behavior either.
- **Qualification:** workload, cost, provider recipients, compute/quotas, metric definitions and the actual Realtime topology remain proposed/unverified. Freeze these before qualifying runs; retain all original numeric gates.

## Source findings and task dependencies

| ID | Evidence / priority | Required correction or verification |
|---|---|---|
| REL-05 | G0; CI config confirmed | `.github/workflows/ci.yml` runs on main pushes, PRs to main and dispatch. Companion ref is `460e58d952ad570c9036bc13118eec66a2591918`; smoke targets configured production URLs. Establish candidate-specific, safe tests and both builds. Inspect targets before invoking CI. B1–B3. |
| SEC-03 | High control gap; refund policy unresolved | `process-refund` accepts active same-business staff and uses `/super/i`; `batch-refund` lacks an action allowlist before service elevation. Batch already rejects suspended/demo callers via `requireAuth` and checks ordinary callers' booking ownership. Resolve policy, validate exact roles and original authority across all entry points/RPCs without removing legitimate OPERATOR/legacy ADMIN flows. D2/C3. |
| SEC-04 | High; source confirmed | Batch log upserts use `body.business_id` and ignore persistence results. Derive audit ownership from validated authority/records; define protected platform or per-tenant audit for global batches. This attribution defect alone does not bypass the existing booking ownership check. D2/E2. |
| COR-07 | High; source confirmed | Pending/manual refund outcomes count as batch successes. Include `app/lib/booking-actions.ts:refundBookingAction`, `app/bookings/page.tsx`, `app/refunds/page.tsx`, server batches and reports in path discovery. Preserve completed/pending/action-required/failed/unprocessed outcomes and original operation IDs. E1/E2. |
| SEC-01 | High; source confirmed | Login requires SHA-256 `password_hash` even for linked Auth users; setup/reset writes it. Migrate to authoritative Auth compatibly, preserving admin/Auth links, legacy recovery and demo login. Do not clear hashes first. D1/C1/C6. |
| SEC-02 | High; source-confirmed race window | Setup completion reads token, changes Auth password, then clears token by admin ID without atomic claim. Current retry path verifies `setup_token_hash_used`; preserve this protection. Prove concurrent claim and failure recovery; recheck public response uniformity, expiry, throttling and trusted origin. No no-token takeover was demonstrated. D1/D4. |
| SEC-11 | Critical regression gate; new protections present | Preserve `read_only` controls in `getCallerAdmin`, shared Edge auth, refund and database boundaries. Verify direct RPC/Storage, service wrappers, stale sessions and alternate customer capabilities; synthetic date refresh and legitimate browsing remain supported. A6/D2/C1. |
| SEC-05 | High verification lead | Two common auth helpers lack MFA assurance checks. Trace remaining API/RLS boundaries and approved sensitive-action policy before declaring global absence; include enrollment/recovery. D1/C1. |
| SEC-06 | High; source confirmed | `proxy.ts` allows an unguarded environment test bypass, Redis without explicit fetch timeout, process-local fallback and five auth attempts per shared IP/15 minutes. Cover actual ingress/Edge/RPC paths, trusted IPs, shared offices, outages and webhook bursts. D4. |
| COR-01 | High; source confirmed | `runBulk()` initializes pending entries, updates separate React objects, then logs every selected ID as succeeded because unchanged `init` entries are never error. Its unused `final` cannot fix this. The browser calls individual actions, not batch-refund; `/refunds` also loops in the browser. Correct actual results and durable accepted-work semantics. E2/C5. |
| COR-02 | High; source confirmed | `cleanupAbandonedVouchers()` ignores send-email HTTP response and DB-update errors before counting sent. Sibling hold-expiry path also ignores email HTTP result. Prove truthful durable state/retries and crash-after-acceptance behavior; reconcile historical markers without mass replay. E1/E3/C4. |
| COR-05 | High; source confirmed | Guide service worker deletes queued check-ins on all 4xx, including 401/429. Preserve recoverable work, explicit reauthentication, tenant/user isolation and visible terminal failures. C6/D5. |
| SEC-07 | Source confirmed | Photo handler verifies tenant ownership but lacks byte/type/size validation before buffering/Drive upload. Include safe names, previews and partial upload/DB failure. D5. |
| SEC-08 | Source hardening need | CSP permits script unsafe-inline/unsafe-eval. This is not proof of XSS; trace real rich-text, URL, redirect, SSRF and CSRF paths; tighten actual responses without breaking integrations. D5. |
| SEC-09 | Source confirmed | Security-drift checks cover public table grants/RLS/policies only; DB TLS uses `rejectUnauthorized: false`. Extend effective column/function/PUBLIC/inherited/view/Storage/search-path checks with low-privilege tests and validated TLS. Do not rubber-stamp a new baseline. D3. |
| SEC-10 | Audit not run | Audit actual lockfiles, deployed runtime and current primary advisories, reachable dependencies, Git/build secrets and CI access. `xlsx` is declared `^0.18.5`; Remotion is present. Preserve media workflows and avoid assumed vulnerability/fix claims. D6. |
| COR-03 | Historical evidence only | September 14 Yoco closeout leaves a fresh payment/notification/refund journey outstanding at that inspection. Obtain current genuine provider evidence and separately approved limited production checks; do not infer today's merchant mode. C2/C3/E1. |
| COR-04 | Verification required | Durable claims/leases/reclaim, backoff, fairness, marketing separation, original-caller authority, all schedulers and delivery state. E3/H3. |
| COR-06 | Verification required | Reconcile inaccurate historical summaries/reminder/payment states with reviewed scope; do not replay history. E4/H5. |
| SCL-01 | Source pattern; not measured | Bookings reload previously visible rows and related batches, including on Realtime refresh. Measure before optimizing; preserve filtering, totals, tenant isolation and updates. H2/C5. |
| SCL-02 | Measurement required | Auth/subscription lookups, reports, query plans/indexes/pools, rendering, caches, Realtime fan-out and revocation. H2/F/G. |
| SCL-03 | Qualification not run | Execute the full approved BT2000-LAUNCH-V2 dataset, identities, guest/job traffic, sockets, thresholded peak/spike/soak, contention and invariant checks. Historical 50-VU reads do not qualify. F/G. |
| REL-01 | Provenance required | Pin actual Admin, booking/onboarding, functions, migrations and configuration in one manifest; inspect deployment separately. B1/H5/H6. |
| REL-02 | Regression required | Upgrade 100 pre-existing synthetic accounts, linked and legacy, with original objects/links/pending work, old tabs, devices and offline queues. C6/H5. |
| REL-03 | External proof required | Restore timings, Auth/encryption/private-object recovery, received alerts and operational ownership. H3/H4/H6. |
| REL-04 | Final gate | Independent review, exact deployed artifacts and approved 24-hour canary after all staging gates. No readiness from saved settings, old CI or mocks. H7/I. |
| REL-06 | Commercial decision required | Resolve the Standard-plan migration's approved existing-customer effects and ledger state as described above. H5/A1/A5/A6. |

Preserve existing refund capture validation, durable reservation, row locks, stable operation/provider idempotency keys and unknown/pending handling. See `supabase/functions/process-refund/index.ts`, `supabase/migrations/20260911160000_refund_operations.sql`, and `supabase/migrations/20260912110000_refunds_require_recorded_capture.sql`. These are source safeguards, not executed proof, and must not be replaced on the assumption that none exist.

After G0, use `11_FIRST_TASK.json` only with resolved refund policy and safe fixtures. If blocked, continue a dependency-ready isolated correction such as truthful bulk outcomes or offline retention. Serialize shared auth/schema/financial contracts. A completed code patch remains distinct from database/provider/deployment qualification.
