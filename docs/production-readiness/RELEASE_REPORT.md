# BookingTours release report

Verdict: `IN_PROGRESS`

The required source was resolved at `feature/launch-rollout-2026-09-20@76d00f3157acb2c73159bef6b8d40a108a9e767d`. The tested Admin application candidate is the accepted descendant `90eed5ee936252077a611b8cbe602c1fc761c301`. No deployment has been performed. The deployed booking companion, onboarding source, deployed app IDs, Edge Function versions, environment identifiers, redacted configuration fingerprint, and production migration ledger are unresolved.

Local source gates currently show a clean install from the candidate lockfile, passing TypeScript, Edge checks, a production build with synthetic public configuration, and 164 passing disposable PostgreSQL isolation checks after the independently reviewed fixture repair and additive reminder-intent migration. Repository-wide lint last had 262 pre-existing warnings; the current targeted task lint has 13 pre-existing warnings and no errors. Final full-unit evidence has 1,176 passes, 3 failures, and 1 skip; the failures are the same companion UI-contract drift against CI's pinned booking commit. These are recorded outcomes, not release waivers.

`GUIDE-OFFLINE-01` / `COR-05` is independently task-approved at `2d6ce982`: offline check-ins are bound to the active user and tenant without per-item bearer tokens; recoverable failures remain queued; terminal failures stay visible; legacy work is quarantined; stale auth generations cannot replay across account switches; bounded batches drain without starvation; and interrupted check-in persistence converges using the existing idempotency key. The original behavioral suite failed 12/12 tests; the final suite passed 26/26. This is isolated unit/build evidence, not proof of a real authenticated browser, database, or deployed journey.

`GUIDE-PHOTO-01` / `SEC-07` is independently task-approved at `a390797`: actual multipart bytes are capped at 4,500,000 bytes, aligned with Vercel's documented 4.5 MB Function request ceiling; files are separately bounded and fully decoded with a pixel limit; unsupported, multi-page, mismatched, malformed, and truncated content is rejected before Drive access; names and MIME are server-derived; and uncertain provider/database outcomes are correlated without destructive cleanup or blind-retry advice. The original focused suite failed 10/12 tests; the final photo suites passed 42/42. HEIC is explicitly unsupported by the verified decoder. Original decoded bytes are retained and handed to Drive's separate-origin preview surfaces; this is not malware certification or content sanitization. No genuine Drive/database/browser test was performed.

`DEMO-AUTH-01` is independently task-approved as a partial `SEC-11` correction at `37f94bc`: shared Next.js and Edge authorization now treats `read_only` as an authority ceiling even if an identity is stale or misconfigured as `SUPER_ADMIN`. An actual auth-helper-to-OTA-GET composition denies foreign tenant selection before service-client access, while own-tenant demo browsing and writable platform support remain available. The original focused regression failed 2/10; final focused and affected suites passed 10/10 and 38/38. This does not certify database/RLS/Storage isolation: existing SQL read authority, the migration ledger/order, low-privilege database tests, and deployed-browser evidence remain open.

`COR-02A` / `COR-02` is independently task-approved at `90eed5e`: voucher reminders now become accepted only after a successful internal-email response with a nonblank provider ID; rejection and outcome uncertainty remain retryable under a server-derived key and the exact persisted provider payload; voucher age is rechecked immediately before submission; and a delayed overlapping worker can replay recorded acceptance without another provider submission or a false failure. The original two-file regression failed 9/87; the final six-file suite passed 115/115 and disposable PostgreSQL passed 164/164. The touched hold-expiry sibling gains the same truthful acceptance and durable intent, but expiry rediscovery, claim/lease fairness, and backlog recovery remain open under `COR-04`. The additive migration is local-only until the target ledger and its order before the locked pricing migration are verified. No genuine email was sent, and provider acceptance is not delivery.

No genuine provider, browser E2E, cross-version 100-account, 2,000-session load, spike, 24-hour soak, restore, monitoring-receipt, deployment, or canary evidence exists. `BT2000-LAUNCH-V2` and its costs remain unapproved. Production operations and live side effects remain unauthorized.

Customer-preservation rules, demo safeguards, current financial invariants, and legitimate role workflows are locked. Refund-role and existing-pricing migration policy require owner decisions before those changes.

`ROLLOUT_READY_2000` is not issued.
